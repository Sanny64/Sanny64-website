export default function Portfolio() {
    return (
        <div className="confidential">
          This page will contain private info, that can only be accessed while logged in. All private info must be handled confidentially and will therefore be saved as github env secrets.
        </div>
    )
}